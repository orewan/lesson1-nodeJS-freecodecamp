const num1 = 5;
const num2 = 10;

function addValues(){
 // Affiche : the sum is 5 + 10, n'effectue pas l'operation
 //console.log(`the sum is ${num1}+${num2}`);

 // Affiche : the sum is 15, effectue l'operation
 console.log(`the sum is : ${num1 + num2}`);
}

addValues();